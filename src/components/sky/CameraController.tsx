/**
 * Camera controller - disabled for now to avoid rotation animation issues.
 * Users can manually navigate using mouse/touch controls.
 */
export default function CameraController() {
  // Camera animation disabled - causes UI lag and disorientation
  return null;
}
